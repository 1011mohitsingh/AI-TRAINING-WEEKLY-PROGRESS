# from oops import Atm
# sbi = Atm() # object is created and the constructor will be automatically executed and the menu will be called and we can do all the operations that are there in the menu okkay. So that is the use of constructor.

# from fraction import Fraction
# x = Fraction(3,4) # object is created and the constructor will be automatically executed and the num and den will be assigned to the values that we have passed in the constructor okkay. So that is the use of constructor.
# y = Fraction(5,6)
# print(x)
# print(y)
# print(x+y)
# print(x-y)
# print(x*y)
# print(x/y)

from Encapsulation import Atm
sbi = Atm()
sbi.get_pin()
sbi.set_pin("1592")